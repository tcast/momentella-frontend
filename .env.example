# Upstream Fastify API (used by Next.js route handlers to proxy /api/auth, /api/client, /api/admin).
MOMENTELLA_API_URL=https://api.example.com

# Public browser origin of this Next app (no trailing slash). Used by the Better Auth client and magic-link callbacks.
NEXT_PUBLIC_APP_URL=http://localhost:3000
